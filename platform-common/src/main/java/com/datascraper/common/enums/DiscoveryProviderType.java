package com.datascraper.common.enums;

/**
 * Discovery provider plugin types — discover companies only, never enrich.
 */
public enum DiscoveryProviderType {
    BUSINESS_DIRECTORY,
    GOVERNMENT_REGISTRY,
    OPEN_DATA,
    INDUSTRY_LISTING,
    BUSINESS_SEARCH_API,
    CATALOG_SEED
}
