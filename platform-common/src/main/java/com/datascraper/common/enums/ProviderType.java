package com.datascraper.common.enums;

/**
 * Data enrichment provider types (plugin architecture).
 * Replaces the former ScraperType for the CompanyDataProvider model.
 */
public enum ProviderType {
    WEBSITE,
    CONTACT,
    GITHUB,
    TECHNOLOGY,
    NEWS,
    SOCIAL
}
