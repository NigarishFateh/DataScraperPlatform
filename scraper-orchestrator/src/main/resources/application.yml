spring:
  application:
    name: scraper-orchestrator
  profiles:
    active: dev
  data:
    redis:
      host: localhost
      port: 6379

server:
  # Gateway owns 8080. Orchestrator is an internal intelligence service.
  port: 8085

scraper:
  resilience:
    timeout-ms: 15000
    max-retries: 3
    retry-delay-ms: 1000
  execution:
    core-pool-size: 4
    max-pool-size: 10
    queue-capacity: 25
    job-timeout-ms: 60000
  cache:
    enabled: false
    ttl-seconds: 3600
    key-prefix: intel:scraper
  services:
    # Target architecture: scrapers by data source (Phase 9+)
    website:
      base-url: http://localhost:8091
    tech:
      base-url: http://localhost:8092
    news:
      base-url: http://localhost:8093
    github:
      base-url: http://localhost:8094
    contact:
      base-url: http://localhost:8095

app:
  cors:
    allowed-origins: chrome-extension://*,http://localhost:*,http://127.0.0.1:*

logging:
  level:
    root: INFO
    com.datascraper: INFO
