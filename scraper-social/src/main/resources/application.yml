spring:
  application:
    name: scraper-social
  profiles:
    active: dev

server:
  port: 8096

social:
  scraper:
    timeout-ms: 15000
    max-items: 25
    respect-robots-txt: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
