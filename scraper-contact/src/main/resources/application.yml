spring:
  application:
    name: scraper-contact
  profiles:
    active: dev

server:
  port: 8095

contact:
  scraper:
    timeout-ms: 15000
    max-items: 25

management:
  endpoints:
    web:
      exposure:
        include: health,info
